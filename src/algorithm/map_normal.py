# Recursively combines all elements of the specified array
# by applying the operation specified in the callback.
# e.g.
# map_normal([1,2,3], x*2 ) => [2, 4, 6]

def map_normal(arr, callback):
    return arr
